CLOUDKIT WEATHER FORECAST

-- Developers --
19127311 - Tran Thanh Tung
19127551 - Huynh Thu Thao

-- Reprequisites --
BEFORE running the code, make sure to do the followings:
1. Install python and pip (used to install libraries).
2. Install all necessary libraries mentioned in the "IMPORTS" section of each python file.
    -> In command prompt, type in: "pip install [library name]".
    NOTE: Some libraries are pre-installed, so reinstallation is unneccessary.
3. Change the SQL Server (DB_SERVER) in ck_Server.py to yours.

-- How to run --
Open command prompt and type in "python ck_[program name].py".